describe('transition', () => {
  it('should be Batman', () => {
    //tag::transition[]
    Array(8).join('wat' - 1) + " Examples!"
    //end::transition[]
  });
  it('should be Batman!', () => {
    console.log(Array(8).join('wat' - 1) + " Examples!")
  });
});